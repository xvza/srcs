#!/usr/bin/env bash
#################################################
declare   "name=$(basename ${BASH_SOURCE[0]%.*})"
#################################################
source    "${name}.sh"  "${SHRD}/${name}"
#################################################
