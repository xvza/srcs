#!/usr/bin/env bash
#################################################
declare   "name=$(basename ${BASH_SOURCE[0]%.*})"
declare   "shrd=${SHRD}/${name}"
#################################################
source    "arrf.sh"  "${shrd}"
#################################################
eval      "$(${name} <(cat ${file[@]}))"
#################################################
