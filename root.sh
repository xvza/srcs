#!/usr/bin/env bash
#################################################
source    "dirs.sh"
declare   "name=$(basename ${BASH_SOURCE[0]%.*})"
#################################################
source    "arrf.sh"   "${srcs[$name]}"
source    <(cat ${file[@]})
#################################################
source    "pkgs.sh"  "${1?}"
eval      "sudo updatedb -v"
#################################################
[[ "${1?}" != "pacman" ]] && return ${?}
#################################################
source    "orph.sh"
#################################################
