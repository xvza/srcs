#!/usr/bin/env bash
#################################################
source    "rofi.sh"
#################################################
declare   "name=$(basename ${BASH_SOURCE[0]%.*})"
#################################################
source    "arrf.sh"  "${SHRD}/${name}"
#################################################
eval      "$(${name} <(cat ${file[@]}))"
#################################################
