#!/usr/bin/env bash
#####################################################
declare   "bin=$(sed -i s/${2?}/${3?}/g ${1?}*)"
declare   "str=sed: replace ${2?} by ${3?} in ${1?}*"
#####################################################
[[ -z ${bin} ]] &&                                  \
echo      "${str}"
#####################################################
[[ -n ${bin} ]] &&                                  \
echo      "${bin}"
#####################################################
