#!/usr/bin/env bash
###########################################################
[[ -z ${3}   ]] && declare "bin=$(cp -vpr ${2?}/* ${1?})"
[[ -n ${3}   ]] && declare "bin=$(cp -vpr ${2?}/.??* ${1?})"
###########################################################
[[ -z ${bin} ]] && echo    "cp: ${1?} directory is newest"
[[ -n ${bin} ]] && echo    "${bin}"
###########################################################
