#!/usr/bin/env bash
#############################################################
source    "colo.sh"
#############################################################
declare   "id=$(id -u ${USER})"
declare   "color=${shell[$id]}"
declare   "init=${shell[init]}"
declare   "bold=${shell[bold]}"
#############################################################
declare   "PS1=\[${bold}\]\[${color}\][\u:\w:\#]\[${init}\] "
#############################################################
